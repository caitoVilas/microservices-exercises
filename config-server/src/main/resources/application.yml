server:
  port: 8001
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/caitoVilas/microservices-exercises
          search-paths: config-data
  application:
    name: config-server
